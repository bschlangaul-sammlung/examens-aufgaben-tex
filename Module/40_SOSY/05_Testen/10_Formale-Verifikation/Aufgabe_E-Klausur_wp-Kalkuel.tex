\documentclass{bschlangaul-aufgabe}
\bLadePakete{java,mathe,wpkalkuel}
\begin{document}
\bAufgabenMetadaten{
  Titel = {Totale Korrektheit IV},
  Thematik = {Totale Korrektheit},
  Referenz = SOSY.Testen.Formale-Verifikation.E-Klausur_wp-Kalkuel,
  RelativerPfad = Module/40_SOSY/05_Testen/10_Formale-Verifikation/Aufgabe_E-Klausur_wp-Kalkuel.tex,
  ZitatSchluessel = sosy:e-klausur,
  ZitatBeschreibung = {Totale Korrektheit IV},
  BearbeitungsStand = unbekannt,
  Korrektheit = unbekannt,
  Ueberprueft = {unbekannt},
}

\let\wp=\bWpKalkuel
\let\equivalent=\bWpEquivalent
\let\erklaerung=\bWpErklaerung

Gegeben sei die folgende Methode:
\footcite[Totale Korrektheit IV]{sosy:e-klausur}

\bJavaDatei[firstline=4,lastline=11]{aufgaben/sosy/totale_korrektheit/Blub}

\noindent
Berechnen Sie hierzu das folgende Kalkül:

\begin{displaymath}
\wp{R}{a \geq 0}
\end{displaymath}

\begin{bAntwort}
$\wp{R}{a \geq 0}$

\equivalent{
  \wp{if (a > 15) \{a = a - 42;\} else \{a = -a;\}}{a \geq 0}
}

%%
%
%%

\erklaerung{Aufteilung der Verzweigung in:\\ \bWpErklaerungVerzweigung}

\equivalent{
  (a > 15 \land \wp{a = a - 42}{a \geq 0})
  \lor
  (a \leq 15 \land \wp{a = -a}{a \geq 0})
}

\erklaerung{Code einsetzen}

\equivalent{
  (a > 15 \land \wp{}{a - 42 \geq 0})
  \lor
  (a \leq 15 \land \wp{}{-a \geq 0})
}

%%
%
%%

\erklaerung{Kein Code mehr verhanden. Wir lassen „wp“ weg.}

\equivalent{
  (a > 15 \land a - 42 \geq 0)
  \lor
  (a \leq 15 \land -a \geq 0)
}

%%
%
%%

\erklaerung{Wir bringen 42 nach rechts und multiplizieren $-a \geq 0$
mit $-1$.}

\equivalent{
  (a > 15 \land a \geq 42)
  \lor
  (a \leq 15 \land a \leq 0)
}

%%
%
%%

\erklaerung{Die Aussagen reduzieren, redundante Aussagen weglassen}

\equivalent{
  (a \geq 42)
  \lor
  (a \leq 0)
}

%%
%
%%

\erklaerung{Weglassen der Klammern}

\equivalent{
  a \geq 42
  \lor
  a \leq 0
}

\end{bAntwort}

\end{document}
