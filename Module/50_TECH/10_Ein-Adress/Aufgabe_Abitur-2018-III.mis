PROGRAM abi;
VAR n;

BEGIN
  n := 15;
  WHILE n >= 3 DO
    IF (n % 4) = 0 THEN
      n := n / 4;
    ELSE
      IF (n % 2) = 0 THEN
        n := n / 2 + 1;
      ELSE
        n := (n + 1) / 2;
      END;
    END;
  END
END abi.
