PROGRAM collatz;
VAR n, anzahl;

BEGIN
  n := 7;
  anzahl := 0;
  WHILE n <> 1 DO
    IF (n % 2) = 0 THEN
      n := n / 2;
    ELSE
      n := 3 * n +  1;
    END;
    anzahl := anzahl + 1;
  END
END collatz.
