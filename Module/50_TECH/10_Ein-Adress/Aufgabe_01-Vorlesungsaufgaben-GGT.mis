PROGRAM ggt;
VAR a, b, ggt;

BEGIN
  a := 10;
  b := 5;
  IF a = 0 THEN
    ggt := b;
  ELSE
    WHILE b <> 0 DO
      IF a > b THEN
        a := a - b;
      ELSE
        b := b - a;
      END;
    END;
    ggt := a;
  END
END ggt.
